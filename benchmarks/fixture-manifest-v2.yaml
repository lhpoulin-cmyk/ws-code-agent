schema: ws-doc-writer-benchmark-fixtures/v2
runner_version: ws-doc-writer-benchmark-runner/v2
frozen_input_contract: benchmarks/fixture-manifest.yaml
# v2 calls v1's verifier, so the original v1 runner hash and all ten fixture,
# cases, prompt, VOICE, scoring, and reproducibility hashes remain the gate.
v1_fixture_manifest_sha256: d7733f19769a8bdde199250c24829186394559e6fd50128524fd803f2bd9ab1d
expected_outputs: 30
controlled_settings:
  context: 8192
  temperature: 0.2
  top_p: 0.9
  seed: 42
  streaming: false
  thinking: disabled
