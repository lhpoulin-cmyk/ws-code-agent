source_repository: ws-doc-writer
source_commit: f46e3f0abb335fc37fe03b6b168ce07f0656cbf9
source_reference: benchmarks/cases.yaml
case_id: reconciliation
extraction_notes: Synthetic fixture based on the three peer contracts; it does not
  override any peer.
payload: 'Source authority: ws-doc-writer owns application benchmark semantics, ws-cp
  owns runtime filesystem mechanics, and gpu-cp owns accelerator acceptance. A prior
  note incorrectly assigned corpus ownership to ws-cp. Reconcile the authorities explicitly.'
required_facts:
- source authority
- contradictions
- resolution
forbidden_inventions:
- silent precedence
- terminology drift
authority_boundaries:
- peer authorities
exact_terminology:
- exact source names
expected_output_type: reconciled technical section
max_output: 500
