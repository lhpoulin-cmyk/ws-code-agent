schema: ws-code-agent-backends/v1
required: [id, display_name, base_url, enabled]
optional: [accelerator_family, host_label]
security:
  - Endpoint configuration is runtime-owned and contains no credentials.
  - SSH forwarding and service ownership remain infrastructure concerns.
