# tests

Intentionally empty. No coding-agent tests exist yet, so the suite collects zero
tests — that is the correct signal, not a failure. Inherited Doc Writer phase
tests were removed in the 2026-08-09 skeleton reduction and remain in Git history.
